# Copy to .env for local `docker compose up` / `task docker:up`. Never commit .env.

# Bearer token Hermes (or any MCP client) sends in the Authorization header to /mcp.
# Leave unset to run without /mcp mounted — the agent still works on its own.
ARGUS_MCP_TOKEN=change-me-to-a-long-random-value

# /breakglass has its own login (no token to set here) — the first visit to /login
# generates an admin account and shows the password once. See the README.

# ntfy.sh topic URL for break-glass push notifications. Leave unset to skip pushes.
ARGUS_NTFY_TOPIC_URL=

# Which Argus config file gets bind-mounted into the container. Point this at your
# own file once you have one; examples/argus.example.yaml is just enough to start.
CONFIG_FILE=./examples/argus.example.yaml

# One process, one port: /agent (AG-UI) and, if ARGUS_MCP_TOKEN is set, /mcp +
# /breakglass are all served here.
AGENT_PORT=8421

# Postgres for the agent's checkpoints/history — the compose file builds the agent's
# ARGUS_AGENT_DATABASE_URL from this directly, don't set that one separately.
POSTGRES_PASSWORD=change-me-to-a-long-random-value

# https://openrouter.ai/keys — one key routes to any model (see agent.model in your
# Argus config, e.g. examples/argus.example.yaml).
OPENROUTER_API_KEY=
